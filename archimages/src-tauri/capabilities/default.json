{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window default capability (least privilege)",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
